import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Hostinger Node.js hosting runs `next start` after `next build`
  // No special output mode needed — standard Node.js server deployment
};

export default nextConfig;
